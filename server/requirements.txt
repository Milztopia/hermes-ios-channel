fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# Optional — PDF text extraction (/api/extract-pdf)
# pip install pdfplumber
pdfplumber>=0.11.0

# Hermes config discovery and debug endpoint
pyyaml>=6.0

# Optional Hermes TTS provider exposed through the iOS channel
edge-tts>=7.0.0
