name: toolset-override
version: 1.2.0
description: "Lets POST /v1/runs carry per-request toolset overrides without editing upgrade-managed Hermes core code."
author: "HermesInterface"
kind: standalone
